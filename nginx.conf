server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://api:3001/api/;
  }
  location /videos/ {
    proxy_pass http://api:3001/videos/;
  }
  location / {
    try_files $uri /index.html;
  }
}
